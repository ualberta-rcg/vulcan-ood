<%# With auth-none, RStudio trusts the OOD proxy; no login form needed %>
<a href="/rnode/<%= host %>/<%= port %>/" class="btn btn-primary" target="_blank">
  🔓 Connect to RStudio Server
</a>

<div class="mt-3">
  <details class="ood-appkit">
    <summary>Installing R packages</summary>
    <p class="mb-1">Packages install to <code>~/.rstudio/&lt;version&gt;/library</code> (one folder per RStudio version).</p>
    <p class="mb-1"><strong>From the R Console:</strong></p>
    <pre class="bg-light p-2 rounded mb-2"><code>install.packages("package_name", repos = "https://cloud.r-project.org", dependencies = TRUE)</code></pre>
    <p class="mb-1"><strong>From the RStudio Terminal</strong> (Tools → Terminal): set the library path first, then use R or Rscript:</p>
    <pre class="bg-light p-2 rounded mb-2"><code>export R_LIBS_USER="$HOME/.rstudio/rstudio-server-4.4/library"
R -e 'install.packages("package_name", repos = "https://cloud.r-project.org", dependencies = TRUE)'</code></pre>
    <p class="mb-0 small text-muted">Use the version folder that matches your session (e.g. <code>rstudio-server-4.4</code>). Check <code>.libPaths()</code> in R to see your path.</p>
  </details>
</div>
