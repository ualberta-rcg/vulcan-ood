<%# With auth-none, RStudio trusts the OOD proxy; no login form needed %>
<a href="/rnode/<%= host %>/<%= port %>/" class="btn btn-primary" target="_blank">
  🔓 Connect to RStudio Server
</a>

<div class="mt-3">
  <p class="mb-1"><strong>Installing R packages (after RStudio is running)</strong></p>
  <p class="mb-2"><code>R_LIBS_USER</code> is already set in the session (Tools → Terminal). Just run:</p>
  <pre class="bg-light p-2 rounded mb-2"><code>R -e 'install.packages("package_name", repos = "https://cloud.r-project.org", lib = Sys.getenv("R_LIBS_USER"), dependencies = TRUE)'</code></pre>
  <p class="mb-0 small text-muted">Replace <code>package_name</code> with the package you want (e.g. <code>tidyverse</code>).</p>
</div>
