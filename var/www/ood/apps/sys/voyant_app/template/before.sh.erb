#!/bin/bash
# before.sh.erb — Voyant setup. Shared preamble from templates/web_setup.sh.
export OOD_APP_NAME="voyant"
<% template_root = "/var/www/ood/apps/templates/" %>
<% IO.foreach(template_root + "web_setup.sh") do |line| %><%= line %><% end %>
echo "[before:${OOD_APP_NAME}] setup complete"
