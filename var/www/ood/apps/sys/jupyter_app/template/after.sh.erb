#!/bin/bash
# after.sh.erb — wait for the Jupyter Lab server (OOD batch_connect 'after' hook).
echo "[after] waiting for Jupyter Lab on ${host}:${port} (timeout 600s)..."
if wait_until_port_used "${host}:${port}" "600"; then
  echo "[after] Jupyter Lab is up on port ${port}"
else
  echo "[after] ERROR: timed out waiting for port ${port} — check output.log"
  clean_up 1
fi
sleep 2
echo "[after] ready"
