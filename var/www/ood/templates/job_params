  num_cores:
    widget: "number_field"
    label: "Number of CPU cores"
    value: 1
    help: "Maximum number of CPU cores is limited by the maximun CPU cores on a node. See [Alliance Cluster Documentation](https://docs.alliancecan.ca/wiki/Technical_support) for host specifications."
    min: 1
    step: 1

  bc_num_hours:
    widget: "number_field"
    label: "Number of hours"
    value: 1
    min: 1
    max: 8
    step: 1
    help: "Maximum walltime for interactive sessions is 8 hours."

  bc_email_on_started:
    widget: "check_box"
    label: "I would like to receive an email when the session starts"
    help: "If you do not receive an email, check your registered address at the [Alliance Support Portal](https://docs.alliancecan.ca/wiki/Technical_support)."

  bc_vnc_resolution:
    required: true

  advanced_options:
    widget: "check_box"
    label: "Advanced options (memory, GPU, nodes)"
    help: "Check to enable memory, GPU, node constraints. Leave unchecked for default resource requests."
    value: "0"

  memtask_checkbox:
    widget: "check_box"
    label: "Customize memory allocation"
    value: "0"  # unchecked by default (means use default system RAM)
    help: |
      - If unchecked, the system automatically selects memory based on the number of CPU cores.
      - If checked, choose the amount of RAM manually.

  memtask:
    widget: select
    label: "Select Memory (GB)"
    value: "2"
    options:
      - [ "2 GB", "2" ]
      - [ "4 GB", "4" ]
      - [ "8 GB", "8" ]
      - [ "16 GB", "16" ]
      - [ "32 GB", "32" ]
      - [ "64 GB", "64" ]
      - [ "128 GB", "128" ]
      - [ "256 GB", "256" ]
      - [ "512 GB", "512" ]
      - [ "1024 GB", "1024" ]

  gpu_checkbox:
    widget: "check_box"
    label: "Enable GPU acceleration"
    value: "0"
    help: |
      - If unchecked, no GPU will be allocated.
      - If checked, select the GPU model manually.

  gpu_type:
    label: "Select GPU Type" 
    widget: select
    value: "none"
    options: []  # populated dynamically

  gpu_count:
    label: "Number of GPUs"
    widget: "number_field"
    value: 1
    min: 1
    max: 8
    step: 1

  add_env_checkbox:
    widget: "check_box"
    label: "Additional environment setup"
    value: "0"
    help: "Custom enviroment commands (e.g., module load, source activate) that will run before your job starts. Use only if needed."  

  additional_environment:
    widget: text_area
    skip_label: true

